development:
  clients:
    default:
      uri: <%= ENV.fetch("MONGODB_URI", "mongodb://localhost:27017/url_shortener_development") %>
      options:
        server_selection_timeout: 5
  options:
    use_utc: true

test:
  clients:
    default:
      uri: <%= ENV.fetch("MONGODB_URI", "mongodb://localhost:27017/url_shortener_test") %>
